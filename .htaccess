RewriteEngine On

# 1. Allow real files and directories to be accessed directly
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# 2. Redirect everything else to index.php
RewriteRule ^(.*)$ index.php [QSA,L]
